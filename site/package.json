{
  "name": "tovian-site",
  "version": "1.0.0",
  "description": "Tovian Language Guide and Dictionary",
  "scripts": {
    "prestart": "node scripts/generate_parallels.js && node scripts/generate_unimorph_verbs.js",
    "start": "eleventy --serve",
    "prebuild": "node scripts/generate_parallels.js && node scripts/generate_unimorph_verbs.js",
    "build": "eleventy"
  },
  "keywords": ["conlang", "tovian"],
  "author": "Jackson Dean",
  "license": "MIT",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.0"
  }
}
