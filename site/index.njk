---
layout: base.njk
title: Tovian | Learn, Search, Ask
---
<section id="home" class="section hero" data-reveal>
  <div class="container grid2">
    <div>
      <div data-reveal>
        <h1>Learn Tovian</h1>
        <h3 title="If we should speak the truth, we will make peace."><i>{% tovscript "we aux_1_sbj_sim_prs speech def.acc.abs-truth, we aux_1_sim_fut make acc.abs-peace" %}</i></h3>
        <h3 title="If we should speak the truth, we will make peace."><i>{% tovc "we aux_1_sbj_sim_prs speech def.acc.abs-truth, we aux_1_sim_fut make acc.abs-peace" %}</i></h3>
        <p>Search the dictionary or browse the complete guide. Quick navigation: <a href="#dictionary">Dictionary</a> · <a href="{{ '/guide/index.html' | url }}">Guide</a></p>
      </div>
      <div data-reveal>
        <h2>Ask</h2>
        <p class="muted">Type a keyword you’d look up (e.g., “questions”, “verbs”, “cases”, “dream”, “memory”). It searches the guide (titles) and dictionary.</p>
        <div class="qa box">
          <input id="askInput" class="input" placeholder="Search grammar or vocabulary (e.g., questions, locative, dream)" />
          <div id="askResults" class="stack"></div>
        </div>
      </div>
    </div>
    <div class="card" data-reveal>
      <h3>Word of the Day <span id="wotdChip" class="chip"></span></h3>
      <div id="wotd"></div>
      <div class="muted" id="dictStats" aria-live="polite"></div>
    </div>
  </div>
</section>

<section id="dictionary" class="section">
  <div class="container">
    <div class="row between center">
      <h2>Dictionary <span id="dictBadge" class="chip" aria-live="polite" title="Loading unique Tovian word count...">0</span></h2>
      <div class="row gap">
        <input id="dictSearch" class="input" placeholder="Filter entries..." />
        <button id="favoritesBtn" class="btn toggle" aria-pressed="false">Favorites ★</button>
        <button id="toggleAuxBtn" class="btn toggle" aria-pressed="false">Include Auxiliaries</button>
        <button id="toggleIpaBtn" class="btn toggle" aria-pressed="true">Show IPA</button>
        <button id="toggleTableBtn" class="btn toggle" aria-pressed="false">Table View</button>
      </div>
    </div>
    <div id="dictCards" class="cards"></div>
    <div class="row center" style="justify-content:center; margin-top:12px">
      <button id="loadMoreBtn" class="btn" aria-live="polite">Load more</button>
    </div>
    <div id="tableWrap" class="table-wrap hidden">
      <div id="dictionaryHolder"></div>
    </div>
  </div>
</section>

<section id="guide" class="section alt" data-reveal>
  <div class="container">
    <h2>Complete Guide</h2>
    <p class="muted">Welcome. <a class="btn" href="{{ '/guide.pdf' | url }}" download>Download full PDF guide</a></p>
    <div id="guideLinks" class="cards"></div>
  </div>
</section>

<section id="examples" class="section" data-reveal>
  <div class="container">
    <h2>Examples</h2>
    <p class="muted">Practice pages with curated sentence examples and patterns.</p>
    <div id="examplesLinks" class="cards"></div>
  </div>
</section>


<section id="about" class="section alt" data-reveal>
  <div class="container">
    <h2>About</h2>
    <p>This placeholder will introduce the Tovian language: its phonology, core grammar ideas, and cultural context. [WIP].</p>
  </div>
</section>
